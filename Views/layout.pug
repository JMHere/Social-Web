doctype
html 
    head 
        link(rel="stylesheet", href="styles.css")
    body 
        header 
            nav
                ul.list 
                    each item in nav.menu 
                        a.link(href=item[1] traget=item[2])
                            li.naver(id=item[0]) #{item[0]}
        section 
            h1 #{title}
            h2=title2
            block content
        footer 